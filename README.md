# 💧⚡ Счетчики ЖКХ (Ростелеком Ключ) для Home Assistant

Привет! Если ваша управляющая компания использует платформу «Ростелеком Ключ», эта интеграция поможет автоматически забирать показания счетчиков (электричество, горячая и холодная вода) прямо в Home Assistant. 

Больше не нужно переписывать цифры с бумажки — система сама найдет ваши приборы учета, создаст для них сенсоры и аккуратно сложит данные в панель «Энергия».

---

##  Установка

Самый простой и правильный путь — через магазин **HACS**.

1. Зайдите в **HACS** -> **Интеграции**.
2. Нажмите на три точки в правом верхнем углу (⋮) и выберите **Пользовательские репозитории**.
3. Вставьте ссылку на этот GitHub-репозиторий и выберите категорию **Интеграция**.
4. Нажмите **Добавить**, затем скачайте появившуюся интеграцию.
5.  **Обязательно перезагрузите Home Assistant!**

*(Если вы не используете HACS, просто скачайте папку `my_meters` из этого репозитория и положите её в папку `custom_components` вашего Home Assistant, а затем перезагрузите систему).*

---

##  Добываем URL и Токен (Где их искать)

Чтобы Home Assistant смог достучаться до ваших данных, ему нужны адрес сервера (API) и ваш пропуск (Токен). Их нужно вытащить из браузера:

1. Откройте личный кабинет «Ростелеком Ключ» на компьютере.
2. Нажмите **F12**, чтобы открыть режим разработчика.
3. Перейдите на вкладку **Network** (Сеть) и включите фильтр **Fetch/XHR** (чтобы скрыть лишние картинки).
4. Зайдите в раздел со счетчиками на сайте (или просто обновите страницу).
5. В появившемся списке ищите запрос следующего вида (у каждого пользователя в конце будут свои уникальные обозначения `indicatorIds`):
   `https://keyapis.key.rt.ru/telemetry_metric/api/v1/measurement/converted/list?...&filter.indicatorIds=XXXXX`
6. Кликните по этому запросу. Справа откроется панель. На вкладке **Headers** (Заголовки) найдите:
   * **Request URL (API):** Это наш адрес. Скопируйте строку целиком.
   * **Authorization (Токен):** Прокрутите чуть ниже. Вы увидите длинную строку вида `Bearer eyJhbG...` (очень длинный набор символов). Скопируйте **только сам код**, без слова `Bearer ` (то есть код должен начинаться с `eyJ...`).

* Важно: Токены безопасности иногда имеют срок годности (по расчётам 1 год). Если сенсоры вдруг перестали обновляться, просто повторите этот шаг и обновите токен в настройках.*

---

##  Запуск в Home Assistant

1. Идем в **Настройки** -> **Устройства и службы**.
2. Жмем синюю кнопку **+ Добавить интеграцию** внизу экрана.
3. Ищем **Мои Счетчики** (или My Meters).
4. Вставляем наши API URL и Токен, добытые на предыдущем шаге.
5. Жмем **Отправить**. Готово! Ваши счетчики добавлены.

---
##  Если цифры не сходятся (Корректировка)

Бывает, что данные в API немного отстают или отличаются от того, что вы видите глазами на счетчике. Это легко поправить:

1. Зайдите в настройки интеграции (**Настройки** -> **Устройства и службы** -> карточка интеграции).
2. Нажмите кнопку **Настроить**.
3. Вы увидите список всех ваших приборов учета. Введите нужную поправку:
   * Счетчик в HA показывает `100`, а в реальности `105`? Пишем `5`.
   * В HA `100`, а в реальности `95`? Пишем `-5`.
4. Сохраните, и значения сенсоров мгновенно пересчитаются с учетом вашей правки!
