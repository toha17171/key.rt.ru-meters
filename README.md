# 💧⚡ Интеграция счетчиков «Ростелеком Ключ» для Home Assistant

Кастомный компонент (Custom Component) для Home Assistant, позволяющий получать показания приборов учета (электричество, горячая и холодная вода) из личного кабинета приложения «Ростелеком Ключ» (и совместимых платформ на базе Doma.ai / Ujin).

Интеграция автоматически находит все ваши счетчики, создает для них правильные сенсоры и позволяет использовать их во встроенной панели **«Энергия» (Energy Dashboard)**.

## ✨ Возможности

* **Настройка через UI:** Никакой правки YAML. Удобный ввод URL и токена прямо в веб-интерфейсе Home Assistant.
* **Поддержка панели «Энергия»:** Сенсоры создаются с правильными классами (`state_class: total_increasing`, `device_class: energy / water`), поэтому HA сразу может строить по ним графики потребления.
* **Умные иконки:** Автоматический подбор иконок (молния, капля, капля с термометром) в зависимости от типа ресурса.
* **Индивидуальная корректировка:** Возможность задать "смещение" (плюс или минус) для каждого счетчика отдельно через настройки интеграции, если сырые данные от API немного расходятся с табло.
* **Автоматическая синхронизация:** Запрос данных происходит раз в час, скрипт автоматически генерирует и подставляет текущее время (`endSyncedAt=now`) в нужном для сервера формате (RFC3339).

---

## 🛠 Установка

### Способ 1: Вручную (HACS Custom Repository) *Рекомендуется*
Если у вас установлен HACS, вы можете добавить этот репозиторий как пользовательский:
1. Откройте **HACS** -> **Интеграции**.
2. Нажмите на три точки в правом верхнем углу -> **Пользовательские репозитории**.
3. Вставьте ссылку на этот репозиторий (GitHub) и выберите категорию **Интеграция**.
4. Нажмите **Установить** и перезагрузите Home Assistant.

### Способ 2: Вручную (копирование файлов)
1. Скачайте архив с кодом интеграции.
2. Скопируйте папку `my_meters` (или как называется ваша папка) в директорию `/config/custom_components/` вашего Home Assistant.
3. Перезагрузите Home Assistant.

---

## 🔑 Как получить URL и Token (Авторизация)

Для работы интеграции необходимо "перехватить" ссылку на API и ваш токен авторизации из веб-версии личного кабинета.

1. Откройте веб-версию личного кабинета «Ростелеком Ключ» в браузере (Chrome, Safari, Яндекс.Браузер) на компьютере.
2. Откройте панель разработчика: нажмите клавишу **F12** (или `Ctrl+Shift+I` / `Cmd+Option+I`).
3. Перейдите на вкладку **Network (Сеть)** и включите фильтр **Fetch/XHR**.
4. Перейдите в раздел **Счетчики** на сайте (или обновите страницу).
5. В списке запросов найдите тот, который возвращает данные счетчиков (обычно называется `meters`, `indications` или содержит длинный набор символов). Кликните на него.
6. Откройте вкладку **Headers (Заголовки)** справа:
   * **URL API:** В самом верху найдите `Request URL`. Скопируйте эту строку целиком (например: `https://api.ххх.ru/v1/meters?...`).
   * **Token:** Прокрутите вниз до раздела `Request Headers`. Найдите строку `Authorization: Bearer eyJhbGciOi...`. Скопируйте **только сам код** (всё, что идет после слова `Bearer `).

*Примечание: Если токен имеет короткий срок жизни (например, 1 сутки), вам придется периодически обновлять его в настройках.*

---

## ⚙️ Настройка в Home Assistant

1. Перейдите в **Настройки** -> **Устройства и службы**.
2. Нажмите кнопку **+ Добавить интеграцию** в правом нижнем углу.
3. Введите в поиске **Мои Счетчики** (или My Meters).
4. В появившемся окне вставьте скопированные ранее **URL API** и **Token**.
5. Нажмите **Отправить**.

Если данные введены верно, интеграция сообщит об успехе и автоматически создаст устройства для всех найденных приборов учета.

---

## 🎛 Корректировка показаний (Options)

Если показания API расходятся с реальными данными на счетчике, вы можете задать смещение:
1. Перейдите в **Настройки** -> **Устройства и службы** -> найдите карточку интеграции.
2. Нажмите кнопку **Настроить** (Configure).
3. В открывшемся окне вы увидите список всех ваших счетчиков. Введите необходимое число для корректировки:
   * Если счетчик в HA показывает `100`, а в реальности `105`, введите `5`.
   * Если в HA `100`, а в реальности `95`, введите `-5`.
4. Нажмите **Отправить**. Сенсоры обновятся мгновенно.

---

## 🐛 Решение проблем

* **Сенсоры перешли в статус "Недоступно" (Unavailable):** Скорее всего, ваш токен устарел ("протух"). Получите новый токен через F12 (как описано выше) и обновите его, удалив и заново добавив интеграцию, или через скрытые настройки `core.config_entries`.
* **Ошибка 500 при настройке корректировки:** Убедитесь, что данные счетчиков успешно загрузились с сервера. Подождите пару минут после установки и попробуйте снова.
* **В логах ошибка "cannot parse time":** Убедитесь, что вы используете последнюю версию файлов интеграции (с правильной обработкой RFC3339 времени в `__init__.py`).
